# Instale a CLI globalmente (requer Node.js):
# sudo apt install npm
# npm install -g @devcontainers/cli

# Execute o comando de rebuild navegando até a raiz do seu projeto (onde está a pasta .devcontainer):
devcontainer up --workspace-folder . --remove-existing-container